简单的栈模拟

边遍历边计算深度

```go
func maxDepth(s string) int {
    maxDep := 0
	leftNum := 0 // 记录当前的左括号 不断遍历其中最大值就是最大深度
	for _, ch := range s {
		if ch == '(' {
			leftNum++
			if leftNum > maxDep {
				maxDep = leftNum
			}
		} else if ch == ')' {
			leftNum--
		}
	}
	return maxDep
}   
```