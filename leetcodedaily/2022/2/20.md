```go
func isOneBitCharacter(bits []int) bool {
    // 第一种以0开头 第二种以1开头 正序遍历只要前n-1个能组成就成立 因为最后一个固定是0了
    i, n := 0, len(bits)
    for i < n - 1 { // 最后一次是倒数第二个
        i += bits[i] + 1
    }
    return i == n - 1
}
```