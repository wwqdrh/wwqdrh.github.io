贪心

num + arr1 + arr2 + ...

要满足 > 1

```go
func minStartValue(nums []int) int {
    sum, minSum := 0, 0
    for _, num := range nums {
        sum += num
        if sum < minSum {
            minSum = sum
        }
    }
    return -minSum + 1
}
```