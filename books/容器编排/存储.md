# 简介

磁盘挂载

docker的挂载分为两种，bind(本机路径)和volume(磁盘挂载，可以是存放在本机或者网络磁盘)

# Flocker

Flocker 是一个开源的可以用作 Docker 化应用的数据卷管理器 。 Flocker 通过 提供数据迁移的工具，能让运维团队运行容器化的有状态服务，如生产环境的 MySQL。 与 Docker 的数据卷和单个服务器绑定的做法不同，一个 Flocker 的数据 卷，又叫作 dataset，具有移植性并可以使用在集群中的任何一个容器上 。

它的设计也非常简单: Flocker包含一个控制节点( control node )，它提供一个服务 API 来管理 Flocker 集群和 Flocker 的卷，然后在集群中的每一个节点上都有一个 Flocker 代理 和一些 Docker的插件。

> 如何安装以及设置查看官网

```bash
docker run -v myvolume:/data --volume-driver flocker image command

docker volume ls

docker volume create -d flocker

docker volume create -d flocker -o  size=75G -o profile=bronze --
name=spark

docker service create \
--cnsotraint 'node.labels.type== sparkmaster' \
--network spark \
--name spark-worker \
--replicas 1 \
--env SPARK\_MASTER\_IP=10.0.0.3 \
--mount type=volume,target=/data,source=spark,volume-driver=flocker\ fsoppelsa/spark-worker
```
