# 简介

在宠物模型里，管理员部署服务器或者虚拟机，或者本书中的容器，并且对其持续进行维护 。需要登录机器或容器，安装软件，完成配置，并且确保一切运转正常 。 因此，这 些机器或者容器是她/他的宠物 。

相对应地，当管理员把基础架构当成牛群模型时，他们并不关心基础架构某个组件的 命运。 她/他不会登录到任何单元里，也不会做手动处理。 相反，他们使用批量方案，借助自动化工具完成部署、配置以及管理 。 如果某个服务器或者容器死机了，它会被自动复活， 或者生成另一个服务器或容器来替代有问题的组件 。 因此，在这种场景下，运维人员管理 的是牛群模型 。



# 云原生

> 在云原生架构时代，我们在谈数字化转型的时候，实质上就是在谈组织架构、应用流程和基础架构设施的转型。近十年来，我们的开发流程从瀑布到敏捷再到DevOps，应用架构从单体到多层再到微服务架构，软件交付与封装经历了物理机、虚拟机再到容器，应用运行的基础设施也从传统数据中心到主机托管再到云计算，技术的变革在多个维度同时进行，云计算、DevOps、容器、微服务架构等技术的成熟与普及，事实上也预示着云原生时代的到来。
> 
> 在云原生时代，企业应该如何构建自己的云原生平台，以支撑其数字化转型过程中的应用云化迁移呢？企业若要从下至上地构建自己专属的云原生平台，其过程将是极其复杂的。我们以完全采用开源技术栈构建云原生平台为例，首先需要通过Linux、OpenStack、Ceph等开源系统和集群软件构建IaaS层，然后再基于Kubernetes和Docker技术来构建PaaS层。事实上难度是在PaaS层的构建上，因为在PaaS层，基于Kubernetes的功能，我们还需要自己实现对多语言运行环境、各类中间件、数据库等服务编目的支持，实现应用的自动构建与部署、基于CI/CD和DevOps的应用全生命周期管理，实现镜像仓库、日志、监控、服务追踪、安全和多租户等集群管理功能，实现基于Web的集群管理和自助服务的极好用户体验，实现对诸如Istio等微服务架构和Knative等Serverless应用架构的支持。

关于云原生实现的想法，具体来说的方向就是利用容器即服务的架构核心，在原始设备(或者现有IaaS平台)的基础上实现PaaS平台(coreos(已被收购)这类的专用os、或者linux平台上的运行工具(自动安装运维必须环境+各类服务管理工具:其实这些组件已有开源实现工具，只需要选型并熟练使用然后运用到自己的平台上))，作为云平台构建的重要一步

## 传统架构

烟囱式应用系统构建，难于共享，资源利用率低，架构不规范

## 云计算

按照类型分类：IaaS、PaaS、SaaS

按照部署方式: 私有云、公有云、混合云

## 传统的云平台架构

传统的架构中IaaS是依靠虚拟化(OpenStack)为基础的，但是依靠 `虚拟化 -> 构建资源池` 的方法存在一些缺点

- 当负载时遇到资源扩展和适配上的限制，难以避开虚拟化层的损耗
- 对于频繁迭代的在线应用难以实现高效Devops所需的更灵活的配置和更细的应用颗粒

## 云原生 2.0

> 现在存在面向弹性应用的云时代的操作系统(cloud Native OS)

指在各种云计算资源环境(包括IaaS、PaaS、SaaS环境)中使用包括：`容器`, `服务网格`, `微服务`, `不可变基础设施`, `声明式API`, 构建和运行可弹性扩展的应用。核心依赖于容器即服务的承载技术。

`架构理念`

- 服务化原则
- 全弹性原则
- 可观测原则
- 韧性原则
- 全自动化原则
- 零信任原则
- 持续演进原则

## PaaS需要哪些服务

### 平台核心

- 持续集成
- 编译打包
- 混合编排部署
- 监控
- 服务集成
- 自动弹性伸缩
- 应用资源调度

### 云中间件服务

- 分布式缓存
- 分布式消息
- 弹性负载均衡

### 微服务运行与治理

- 服务路由
- 服务发现
- 服务注册
- 服务治理
- 服务监控
- RPC SDK

## 现有的PaaS解决方案

> 仅做考察，最终实现不能依靠现有的框架

`OpenShift`

> OpenShift(前身是coreos)是由RedHat推出的企业级Kubernetes平台，其主要目标是构建以OCI（Open Container Initiative）容器封装和Kubernetes容器集群管理为核心，对应用生命周期进行管理并实现DevOps工具链等完整功能的开源容器PaaS平台。OpenShift对应用的持续开发、多租户部署和安全管控等进行了优化，并在Kubernetes的基础上增加了以开发人员和操作管理为中心的工具集，以便实现应用程序的快速开发、轻松部署、简单扩展和全生命周期的维护。OpenShift在上游开源社区的版本名称是OKD（最初叫Origin），OKD版本与Kubernetes发行版本相对应，如OKD 1.10对应Kubernetes 1.10。

## CNCF组织

### 毕业项目

- containerd: 容器运行时，docker引擎默认运行时
- CoreDNS: DNS server
- Envoy: 云原生、高性能、服务代理
- etcd: 分布式键值存储
- fluentd: 日志层
- Harbor: 服务注册、发现工具
- Helm: k8s包管理工具
- Jaeger: 分布式链路追踪工具
- Kubernetes: 关于容器的自动部署管理工具
- Linkerd: 服务网格
- Open Policy Agent: 权限引擎
- Prometheus: 资源监控等
- Rook: 针对k8s的存储服务
- The Update Framework: 安全软件更新系统
- TiKV: 分布式、事务、键值存储
- Vitess: 针对mysql纵向伸缩的数据库集群系统
