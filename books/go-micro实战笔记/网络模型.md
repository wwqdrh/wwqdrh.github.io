掌握了网络是理解微服务第一步

> 注意使用go-micro的时候关闭防火墙

# mdns协议

当mDNS客户机需要解析主机名时，它发送一个IP多播查询消息，要求具有该名称的主机标识自己。目标机器然后多播包含其IP地址的消息。然后，该子网中的所有机器都可以使用这些信息来更新它们的mDNS缓存。任何主机都可以通过发送一个活时间(TTL)等于零的响应包来放弃对某个名称的声明。默认情况下，mDNS只解析以.local顶级域名结尾的主机名。

如果.local包含不实现mDNS但可以通过传统的单播DNS服务器找到的主机，则会导致问题。解决此类冲突需要更改网络配置，而mDNS旨在避免这种更改。
