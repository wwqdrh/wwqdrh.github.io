
``` Python

import functools
import random

" ".join(map(lambda i: "{0:02d}".format(i), functools.reduce(lambda l, r: l[1].append(random.randint(l[0], r)) or (l[1][-1]+1, l[1]), range(24, 31), (1, []))[1]))
```