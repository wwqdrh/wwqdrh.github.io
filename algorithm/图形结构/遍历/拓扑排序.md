用于处理有向无环图的问题，存在dfs和bfs两种版本

1、正向+入度表 2、反向+出度表

```go
func bfsFindOrder(nums int, graph [][]int) (res []int) {
     // nums: 节点个数
    // graph: 图的节点连接状态
    
    var (
        edges = make([][]int, nums) // 反向连接表
        outdeg = make([]int, nums) // 各个节点的出度个数
    )
    
    for _, info := range graph {
        edges[info[1]] = append(edges[info[1]], info[0])
        outdeg[info[0]]++
    }
    
    // 从出度为0的开始
    st := []int{}
    for i, item := range outdeg {
        if item == 0 {
            st = append(st, i)
        }
    }
    for len(q) > 0 {
        cur := st[0]
        st = st[1:]
        res = append(res, u)
        
        // 将上一层的所有连接出度减1
        for _, v := range edges[cur] {
            outdeg[v]--
            if outdeg[v] == 0 {
                st = append(st, v)
            }
        }
    }
    if len(res) != nums {
        return []int{}
    }
    return st
}
```